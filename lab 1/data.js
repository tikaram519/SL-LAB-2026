const countries = ["India", "Usa", "Austrialia", "Japan"];
const dataList =  document.getElementById("dataList");

for(name of countries) 
{
    dataList.innerHTML += `<li>${name}</li>`;
}
{
    dataList.innerHTML += `<ul> ${name}</ul>`;
}

